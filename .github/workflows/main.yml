name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  test:
    runs-on: php:cli-alpine
    steps:
      - uses: actions/checkout@v2
      - name: Execute tests
        run: make test
